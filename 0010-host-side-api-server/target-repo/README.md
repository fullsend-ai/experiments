# Placeholder target repo

This directory is used as the `--target-repo` argument for `fullsend run`.
The agent's actual work (cloning repos, building images) happens via the
host-side API servers, not in this directory.
